# Jupify

(jup + notify) -> Jupify

To install dependencies:

```sh
bun install
```

To run the server:

```sh
bun run dev
```

To run the web-scraper:

```sh
bun run scrape
```

open http://localhost:3000
