{
  "name": "jupify-cron",
  "module": "index.ts",
  "type": "module",
  "entry": "index.ts",
  "scripts": {
    "dev": "bun run --hot index.ts",
    "build": "bun build index.ts --outdir ./dist --target bun",
    "start": "bun run dist/index.js"
  },
  "dependencies": {
    "cron": "^3.1.7",
    "ky": "^1.7.2",
    "puppeteer": "^23.5.3"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
