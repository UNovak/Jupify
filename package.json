{
  "name": "jupify",
  "scripts": {
    "dev": "bun run --hot server/index.ts",
    "scrape": "bun run utils/scraper.ts",
    "build": "bun build server/index.ts --outdir ./dist --target bun",
    "start": "bun run dist/index.js"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.3.3",
    "prettier-plugin-tailwindcss": "^0.6.8"
  }
}
